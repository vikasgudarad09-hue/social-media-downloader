# Social Media Downloader API - Environment Configuration

# Port to listen on (Railway / Render injects this automatically)
PORT=8000

# Optional: Firebase Admin Service Account (paste single-line JSON string or path)
# FIREBASE_SERVICE_ACCOUNT_KEY='{"type":"service_account","project_id":"..."}'
# FIREBASE_SERVICE_ACCOUNT_PATH="firebase-credentials.json"
